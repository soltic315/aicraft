# AGENTS.md — AiCraft エージェント指示書

このファイルは AI エージェント（GitHub Copilot 等）向けの行動規範を定義します。
コードに変更を加えた場合、以下のルールに従ってドキュメントを更新してください。

---

## ドキュメント更新ルール

### CHANGELOG.md

**必ず更新するタイミング:**
- 新機能の追加
- バグ修正
- 既存機能の変更・削除
- パフォーマンスの改善
- 依存パッケージのバージョン変更

**フォーマット:**
[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式を厳守し、
[Semantic Versioning](https://semver.org/lang/ja/) に従ってバージョンを付与する。

```
## [X.Y.Z] - YYYY-MM-DD

### Added     # 新機能
### Changed   # 既存機能の変更
### Deprecated # 非推奨化
### Removed   # 削除
### Fixed     # バグ修正
### Security  # セキュリティ修正
```

---

### GAME_DESIGN.md

**必ず更新するタイミング:**
- ブロック種類の追加・変更・削除（`blocks.js`）
- プレイヤー操作の追加・変更（`player.js`）
- ワールド生成ロジックの変更（`world.js`, `noise.js`）
- UI / HUD の変更
- ゲームルールやバランスの変更

**留意点:**
- 表形式（Markdown テーブル）で一覧を管理している箇所は必ず同期すること
- 操作説明テーブル・ブロック一覧テーブルを最新状態に保つこと

---

### README.md

**必ず更新するタイミング:**
- 技術スタック（依存ライブラリ・ビルドツール）の変更
- セットアップ手順・必要環境の変更
- 起動・ビルドコマンドの変更
- プロジェクト構成（ディレクトリ構成等）の大幅な変更
- 新たな外部ツールや設定ファイルの追加

**留意点:**
- コマンド例は実際に動作するものだけを記載すること
- 技術スタックの表は `package.json` の内容と常に一致させること

---

## 対象ファイルと担当ドキュメントの対応表

| 変更ファイル | CHANGELOG | GAME_DESIGN | README |
|---|:---:|:---:|:---:|
| `src/blocks.js` | ✅ | ✅ | — |
| `src/player.js` | ✅ | ✅ | — |
| `src/world.js` | ✅ | ✅ | — |
| `src/noise.js` | ✅ | — | — |
| `src/main.js` | ✅ | 場合による | 場合による |
| `package.json` | ✅ | — | ✅ |
| `vite.config.js` | ✅ | — | ✅ |
| `index.html` | ✅ | 場合による | — |

---

## 全般的な注意事項

- **ドキュメントのみ更新する場合は CHANGELOG.md を更新しない**（コード変更が伴う場合のみ）
- 新しい課題・調整項目・フォローアップが見つかった場合は、実装の有無に関わらず `TODO.md` に追記して管理すること
- 日本語で記述すること（コードコメントを除く）
- CHANGELOG の日付は `YYYY-MM-DD` 形式（JST 基準）で記入すること
- バージョン番号はパッチ `0.0.x`、マイナー `0.x.0`、メジャー `x.0.0` の順に上げること
  - **パッチ:** バグ修正・微調整
  - **マイナー:** 後方互換のある新機能追加
  - **メジャー:** 後方非互換の変更・大規模リファクタリング
